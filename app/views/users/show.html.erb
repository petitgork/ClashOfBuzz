<div class="dashboard-container">
  <div>
    <h1>TABLEAU DE BORD</h1>
    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Aenean aliquam varius felis, gravida varius est porta et. Vestibulum ante ipsum primis in faucibus orci luctus et ultrices posuere cubilia curae;</p>
  </div>
  <%= link_to "+ Nouveau tournoi", new_tournament_path, class: "button" %>
  <div class="dashboard-tournaments-container">
    <div>
      <h3>Mes tournois</h3>
      <div class="dashboard-tournaments-buttons">
        <% @user.tournaments.each do |tournament| %>
          <p><%= link_to tournament.name, tournament_path(tournament), class: "button" %></p>
        <% end %>
      </div>
    </div>
    <div class="dashboard-tournaments-details-container">
      <div>
        <div class="dashboard-card">
          <p class="dashboard-title">MON EQUIPE</p>
          <% @user.tournaments.each do |tournament| %>
            <% tournament.teams.each do |team| %>
              <% if team.user == current_user && tournament.id == @user.tournaments.first.id %>
                <h6><%= team.name %></h6>
                <% if team.politics.count > 0 %>
                  <div class="dashboard-card-list">
                    <% team.politics.each do |politic| %>
                      <div class="dashboard-card-politic">
                        <div class="dashboard-card-politic-name">
                          <p class="last-name"><%= politic.last_name %></p>
                          <p class="first-name"><%= politic.first_name %></p>
                        </div>
                        <div>
                          <%= image_tag(politic.avatar, class: "dashboard-card-list-avatar")  %>
                        </div>
                        <p class="dashboard-card-politic-in-game">IN GAME</p>
                      </div>
                    <% end %>
                  </div>
                <% end %>
              <% end %>
            <% end %>
          <% end %>
        </div>
        <div class="button">VOIR +</div>
      </div>
      <div>
        <div class="dashboard-card">
          <p class="dashboard-title">CALENDRIER</p>
        </div>
        <div class="button">VOIR +</div>
      </div>
      <div>
        <div class="dashboard-card">
          <p class="dashboard-title">CLASSEMENT</p>
        </div>
        <div class="button">VOIR +</div>
      </div>
    </div>
  </div>
</div>
